# Скопируйте в keystore.properties (в каталог android/) и заполните локально.
# Файл keystore.properties в git не коммитить.
#
# storeFile — путь относительно каталога android/ (например release.keystore)
storeFile=release.keystore
storePassword=changeme
keyAlias=lifeos
keyPassword=changeme
